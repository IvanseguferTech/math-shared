name: Code coverage
run-name: Code coverage

on:
    pull_request:
        branches: [ "main" ]

    workflow_dispatch:

jobs:
    code_coverage:
        name: Code Coverage
        uses: IvanseguferTech/CommonAnalysis/.github/workflows/code-coverage.yml@main
        with:
            min_limit: 60
            max_limit: 80
